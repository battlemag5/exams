# Математический анализ. Семестр 4.
## Контрольная работа 1. Вариант 2.

Пусть
$$F(\alpha) = \int_{0}^{1} \frac{e^{-\alpha^2 x} \sqrt{1 - x}}{(\alpha^2 + 1) \ln x} \, dx.$$

1. Найдите все значения параметра $\alpha$, при которых интеграл сходится.

2. Докажите, что функция $F(\alpha)$ непрерывна по $\alpha$ в области сходимости интеграла.

Пусть
$$G(\alpha) = \int_{0}^{\infty} \frac{e^{-\alpha x} - e^{-2x}}{x} \cos 3x \, dx.$$

3. Найдите все значения параметра $\alpha$, при которых интеграл сходится.

4. Докажите, что функция $G(\alpha)$ дифференцируема по параметру $\alpha$ внутри области сходимости интеграла. Выпишите $G'(\alpha)$ в виде интеграла.

5. Найдите явный вид функций $G'(\alpha)$ и $G(\alpha)$.

6. Вычислите интеграл
$$\int_{0}^{1} \sqrt{\frac{x}{1 - x}} \frac{dx}{(3 - x)^2},$$
используя эйлеровы интегралы.