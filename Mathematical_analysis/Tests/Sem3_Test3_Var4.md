# Математический анализ. Семестр 3. Контрольная работа 3. Вариант 4.

1. Измените порядок интегрирования:

$$
\int_{\pi/2}^{3\pi/4} dx \int_{\text{ctg}(x)}^{0} f(x, y) dt + \int_{3\pi/4}^{\pi} dx \int_{\text{tg}(x)}^{0} f(x, y) dy
$$

2. Используя полярную замену переменных, вычислите интеграл по области $D = \{ (x,y) \in \mathbb{R}^2 : 4y < x^2 + y^2 < 6y, \ x < y \}$:

$$
\iint_D x \ dx dy
$$

3. Используя подходящую замену переменных, найдите площадь плоской фигуры, ограниченной линиями:
<p align="center">
$y = x^2, \quad y = 16x^2, \quad xy^2 = 1, \quad xy^2 = 4$
</p>

4. Вычислите интеграл:

$$
\iiint_G z \ dx dy dz
$$

где $G$ — область в $\mathbb{R}^3$, ограниченная поверхностями $x^2 + y^2 = 1$, $z = 1$, $z = x + y$, $x > 0$, $y > 0$.
