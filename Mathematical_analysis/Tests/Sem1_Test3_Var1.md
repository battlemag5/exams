# Семестр 1. Контрольная работа 3. Вариант 1.

1. Найдите предел с помощью асимптотических разложений (формулы Тейлора):

$$
\lim_{x \to 0} \frac{\ln(1 + \sin^2 x)}{\sqrt[4]{1 + x^2 + x^3} - 1}
$$

2. Докажите, что не существует предела при $x \to 0$ у функции:
<p align="center">
$f(x) = e^{\cos(1/x^2)}$
</p>

3. Функция $f(x)$ непрерывна в точке $x_0$, и в любой окрестности этой точки имеются как значения $x$, в которых функция положительна, так и значения $x$, в которых функция отрицательна. Найдите значение $f(x_0)$.

4. Используя правило Бернулли-Лопиталя, вычислите предел:

$$
\lim_{x \to 0} (\cos x)^{\frac{1}{x^2}}
$$

5. Найдите производную функции и упростите полученное выражение:

$$
f(x) = \ln(1 + e^{-2x}) + e^x \cdot \text{arctg}(e^{-x})
$$

*(Примечание: в исходном выражении $x-1$ перед логарифмом, вероятно, является опечаткой сканирования/распознавания структуры $\ln(1+...)$)*

6. Выясните, при каких значениях параметров $a$ и $b$ можно доопределить функцию $f(x)$ в точке $x = 0$ так, чтобы она стала:  
а) непрерывной на всей числовой оси;  
б) дифференцируемой на всей числовой оси;  
в) дважды дифференцируемой на всей числовой оси.  

Найдите все существующие производные в точке $x = 0$ для каждого случая.

$$
f(x) = \cases{ a + b \cos x, & $x < 0$ \cr \ln(1 + x) - x, & $x > 0$ }
$$
